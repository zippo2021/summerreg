{% extends "dashboard/base.html" %}
{% block content %}
{% load staticfiles %}
<form method="post">
      {% csrf_token %}
      <table>
      	{% for field in form %}
        <tr>        
        <div class="fieldWrapper">
                <td>{{ field.errors }}
                <b>{{ field.label }}</b> 
                {% if  field.html_name  == 'avatar'  %} 
                
                {% if field.avatar %}<img src="{{ field.value.url }}" /> {% endif %}
                {% endif %}  
                :</td><td>{{ field }}</td>              
        </div>
        </tr>
        {% endfor %}
        <tr>
          <td colspan="2">
            <input type="submit" name="login" value="Отправить измененные данные"/>
          </td>
        </tr>
      </table>
    </form>

{% endblock %}
